# Application dependencies

require:
    - play
    - play -> secure
    - play -> crud
    - provided -> json 1.1.1

repositories:
  - provided:
      type:       local
      artifact:   ${application.path}/jar/[module]-[revision].jar
      contains:
        - provided -> *